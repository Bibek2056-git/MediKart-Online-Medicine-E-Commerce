# MediKart Contacts

This project includes a contact feature that allows users to send messages to the admin and other users. The contact system supports message creation, retrieval, updating, and deletion for efficient communication and management.

## Features
- Users can send messages via the contact form
- Admin can view, update, and delete messages
- All messages are stored in the database

For more details, see the source code in the `src` directory.
